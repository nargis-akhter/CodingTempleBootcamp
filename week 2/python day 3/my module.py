# Calculate the length of a iterable object

def length(user_object):
    return len(user_object)

# Create a set from a object
def my_set([object_1]):
    return object_1

# Create a range function with start, stop, and step
def range_func(s_top, s_tart = 0, s_tep = 1):
    for val in range(s_tart, s_top, s_tep):
        print(val)

    
