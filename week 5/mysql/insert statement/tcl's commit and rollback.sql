#commit - saves the transaction in the database (changes cannot be undone)

#rollback - allows you to take a step back (last changes made will not count)
#rollback will have an effect on the last execution, cannot restore data to an earlier commit